import {LayoutBase} from '../../shared/layouts';

function Dashboard() {
    return (
        <LayoutBase titulo='Página inicial'>
            <span>childreb</span>
        </LayoutBase>
    );
}

export default Dashboard;